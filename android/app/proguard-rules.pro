# Add project-specific ProGuard rules here.
# Flutter plugin AARs ship their own consumer-rules.pro (merged automatically
# by AGP), so this stays empty unless minification actually strips something
# needed — verify by installing and exercising the release build before
# assuming this is enough.
