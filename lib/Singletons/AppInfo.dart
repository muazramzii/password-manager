/// Keep in sync with the `version:` line in pubspec.yaml -- there is no
/// runtime plugin wired up to read it automatically, so this is the one
/// other place that needs updating on a version bump.
class AppInfo {
  static const String version = '1.0.0';
}
